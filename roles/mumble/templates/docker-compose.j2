version: '3.1'
services:
  mumble:
    image: sudoforge/mumble-server:latest
    restart: always
    ports:
      - '127.0.0.1:${TCPPORT}:64738/tcp'
      - '127.0.0.1:${UDPPORT}:64738/udp'
    environment:
      - SUPERUSER_PASSWORD=${SUPERUSER_PASSWORD}
      - MUMBLE_ENABLESSL=1
      - MUMBLE_ALLOWPING=false
      - MUMBLE_USERS=${USERS}
      - MUMBLE_REGISTERNAME=${SERVERNAME}
    volumes:
      - ./data:/data
