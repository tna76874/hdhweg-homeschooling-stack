---
- name: Basic Setup
  hosts: schoolstack
  become: yes
  tags:
    - base
  vars_files:
    - vars.yml
  roles:
    - base

- name: Rocketchat
  hosts: schoolstack
  become: yes
  vars_files:
    - vars.yml
  tags:
    - rocketchat
  vars:
    nginx_domain_name: "chat.{{ main_domain }}"
  roles:
    - nginx
    - nginx-tls-add
    - docker
    - rocketchat

- name: Whiteboard
  hosts: schoolstack
  become: yes
  vars_files:
    - vars.yml
  tags:
    - whiteboard
  vars:
    nginx_domain_name: "tafel.{{ main_domain }}"
  roles:
    - nginx
    - nginx-tls-add
    - docker
    - whiteboard

- name: Jitsi
  hosts: schoolstack
  become: yes
  vars_files:
    - vars.yml
  tags:
    - jitsi
  vars:
    nginx_domain_name: "jitsi.{{ main_domain }}"
  roles:
    - nginx
    - nginx-tls-add
    - docker
    - jitsi

- name: Cryptpad
  hosts: schoolstack
  become: yes
  vars_files:
    - vars.yml
  tags:
    - cryptpad
  vars:
    nginx_domain_name: "cryptpad.{{ main_domain }}"
  roles:
    - nginx
    - nginx-tls-add
    - docker
    - cryptpad

- name: Mumble
  hosts: schoolstack
  become: yes
  vars_files:
    - vars.yml
  tags:
    - mumble
  vars:
    domain_name: "mumble.{{ main_domain }}"
  roles:
    - docker
    - mumble

- name: Watchtower
  hosts: schoolstack
  become: yes
  vars_files:
    - vars.yml
  tags:
    - watchtower
  roles:
    - docker
    - watchtower

- name: Start the most important docker containers
  hosts: schoolstack
  become: yes
  vars_files:
    - vars.yml
  tags: [ 'never', 'startdockers' ]
  roles:
    - docker
    - dockercomposeup
